# PaySuite merchant credentials. Configure real values only in the hosting
# environment; never commit them.
PAYSUITE_API_TOKEN=
PAYSUITE_WEBHOOK_SECRET=
PAYSUITE_API_URL=https://paysuite.tech/api/v1
